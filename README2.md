This app is built using React to display movies.

Tailwind CSS is used for styling the entire application.

React Router DOM is used to handle page routing and navigation.

Users can rate movies using the star rating component.

Double-clicking on a movie card will open the detailed view of that movie.

A search input bar allows users to search for movies by name.